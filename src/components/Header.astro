---
interface Props {
  lang?: "ro" | "ru";
  currentPath?: string;
}

const { lang = "ro", currentPath = "/" } = Astro.props;

const navLinks =
  lang === "ro"
    ? [
        { href: "/", label: "Acasă" },
        { href: "/#servicii", label: "Servicii" },
        { href: "/serviciiBlog/dezvoltare-web", label: "Detalii" },
        { href: "/preturi", label: "Prețuri" },
        { href: "/contact", label: "Contact" },
      ]
    : [
        { href: "/ru", label: "Главная" },
        { href: "/ru#servicii", label: "Услуги" },
        { href: "/ru/serviciiBlog/dezvoltare-web", label: "Детали" },
        { href: "/ru/preturi", label: "Цены" },
        { href: "/ru/contact", label: "Контакты" },
      ];

const homeLink = lang === "ro" ? "/" : "/ru";
const ariaLabel =
  lang === "ro" ? "moldovawebsite.md - Acasă" : "moldovawebsite.md - Главная";
const navAriaLabel =
  lang === "ro" ? "Navigare principală" : "Основная навигация";
const langToggleAriaLabel = lang === "ro" ? "Selectare limbă" : "Выбор языка";
const hamburgerAriaLabel = lang === "ro" ? "Deschide meniul" : "Открыть меню";
---

<header>
  <div class="container nav-container">
    <a href={homeLink} class="logo" aria-label={ariaLabel}>
      <div class="logo-icon" aria-hidden="true"></div>
      moldovawebsite.md
    </a>

    <ul
      class="nav-menu"
      id="nav-menu"
      role="navigation"
      aria-label={navAriaLabel}
    >
      {
        navLinks.map((link) => (
          <li>
            <a href={link.href} class="nav-link">
              {link.label}
            </a>
          </li>
        ))
      }
    </ul>

    <div class="language-toggle" aria-label={langToggleAriaLabel}>
      <button
        class={`lang-btn ${lang === "ro" ? "active" : ""}`}
        aria-pressed={lang === "ro" ? "true" : "false"}>RO</button
      >
      <button
        class={`lang-btn ${lang === "ru" ? "active" : ""}`}
        aria-pressed={lang === "ru" ? "true" : "false"}>RU</button
      >
    </div>

    <button
      type="button"
      class="hamburger"
      aria-label={hamburgerAriaLabel}
      aria-expanded="false"
      aria-controls="nav-menu"
    >
      <span></span><span></span><span></span>
    </button>
  </div>
</header>
